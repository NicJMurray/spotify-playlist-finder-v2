{
  "name": "spotify-playlist-finder-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Spotify Playlist Finder app and Cloudflare Pages Function.",
  "scripts": {
    "start": "python3 -m http.server 5173",
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name spotify-playlist-finder-v2 --branch main"
  },
  "devDependencies": {
    "wrangler": "^4.88.0"
  }
}
