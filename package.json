{
  "name": "spotify-playlist-finder-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prototype front-end for exploring Spotify playlists via a custom search API.",
  "scripts": {
    "start": "python3 -m http.server 5173",
    "dev": "wrangler pages dev .",
    "dev:homepage": "wrangler pages dev . --cwd homepage",
    "deploy:homepage": "wrangler pages deploy . --project-name homepage --cwd homepage"
  },
  "devDependencies": {
    "wrangler": "^4.88.0"
  }
}
